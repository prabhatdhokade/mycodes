# Core (no hard LLM dependency; optional backends loaded if available)
typing-extensions>=4.8.0

# Optional LLM backend (enable by setting OPENAI_API_KEY and installing)
# openai>=1.30.0

# Optional LangGraph/LangChain integration (the architecture mirrors
# LangGraph patterns; install these to run the LangGraph adapter)
# langgraph>=0.2.0
# langchain-core>=0.3.0

# Optional UI (uncomment to use Gradio instead of CLI)
# gradio>=4.44.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
